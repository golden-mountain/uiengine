const reqConfig = {
  pathPrefix: "../../test",
  devMode: true,
  dataSchemaPrefix: "data/schema/",
  // mockDataPrefix: "data/",
  dataPathPrefix: "data/",
  layoutSchemaPrefix: "layouts/"
};

export default reqConfig;
