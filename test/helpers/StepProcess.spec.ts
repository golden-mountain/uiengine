/* global describe, it, before */
import chai from "chai"
import chaiSpies from "chai-spies"
import _ from "lodash"

import { StepProcess } from '../../src'

chai.use(chaiSpies)
const expect = chai.expect

describe('StepProcess', () => {
  before(() => {
  })
  after(() => {
  })
})
